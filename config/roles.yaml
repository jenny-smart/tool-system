roles:
  admin:
    systems:
      - vip
      - daily_scheduler
      - monthly_scheduler
      - field_daily_schedule
      - service_schedule
      - invoice_center
    pages:
      - report
      - log
      - settings
    logs:
      - daily_scheduler
      - field_daily_schedule
      - performance_report
      - service_schedule

  field_only:
    systems:
      - field_daily_schedule
    pages:
      - main
      - log
    logs:
      - field_daily_schedule

  # 如果有其他角色（manager、staff 等），在下方依需求加入 invoice_center
  # manager:
  #   systems:
  #     - invoice_center
  #   pages:
  #     - log
  #   logs: []
