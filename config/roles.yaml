roles:
  admin:
    systems:
      - vip
      - daily_scheduler
      - monthly_scheduler
      - field_daily_schedule

    pages:
      - main
      - report
      - log
      - settings

    logs:
      - daily_scheduler
      - field_daily_schedule
      - performance_report

  field_only:
    systems:
      - field_daily_schedule

    pages:
      - main
      - log

    logs:
      - field_daily_schedule
